using UnityEngine;
using System.Collections;

public class Relocate5 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.position = new Vector3(0, 7.5f, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
