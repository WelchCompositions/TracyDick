using UnityEngine;
using System.Collections;

public class Relocate4 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.position = new Vector3(-1.2f, 7.8f, -.5f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
