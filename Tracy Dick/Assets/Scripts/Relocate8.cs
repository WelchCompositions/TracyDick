using UnityEngine;
using System.Collections;

public class Relocate8 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.position = new Vector3(14f, 11.5f, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
