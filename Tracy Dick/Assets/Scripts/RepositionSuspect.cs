using UnityEngine;
using System.Collections;

public class RepositionSuspect : MonoBehaviour {

	// Use this for initialization
	void Start () {
        
	}
	
	// Update is called once per frame
	void Update () {
        transform.position = new Vector3(57.8f, -5f, -7f);
	}
}
