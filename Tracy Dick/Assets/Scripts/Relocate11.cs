using UnityEngine;
using System.Collections;

public class Relocate11 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.position = new Vector3(0, 5, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
