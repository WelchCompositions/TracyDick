using UnityEngine;
using System.Collections;

public class Relocate6 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.position = new Vector3(9.5f, 11.4f, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
