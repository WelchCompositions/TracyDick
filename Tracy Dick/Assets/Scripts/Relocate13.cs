using UnityEngine;
using System.Collections;

public class Relocate13 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.position = new Vector3(11f, 11.5f, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
